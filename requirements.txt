playwright>=1.45.0
watchdog>=4.0.0        # vigilancia de sistema de archivos (Semana 3)
PyMuPDF>=1.24.0        # validación y conversión de PDFs (Semana 3)
ezdxf>=1.1.0           # lectura/escritura DWG/DXF — corrección automática de texto
pypdf>=4.0.0           # validación firma PDF + ensamblado PDFs municipalidad
# sqlcipher3-binary sólo publica wheels para Linux. En Windows: o se compila
# desde fuente (MSVC + libsqlcipher) o se corre en modo dev con
# CATASTRO_BOT_DEV_MODE=1 (BD sin cifrar — NO usar en producción).
sqlcipher3-binary>=0.5.2; sys_platform == "linux"
pywin32>=306; sys_platform == "win32"
keyring>=24.3.0
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
anthropic>=0.30.0
APScheduler>=3.10.4
requests>=2.31.0
pydantic>=2.7.0
python-dotenv>=1.0.1
cryptography>=42.0.0
# Servicio de fondo + dashboard web
flask>=3.0.0
waitress>=3.0.0          # WSGI server estable para Windows (no usar dev server de Flask)
tenacity>=8.2.0          # Retry/backoff para llamadas externas (Green API, Anthropic)
